cmake_minimum_required (VERSION 3.5)

# Add sub directories
add_subdirectory(logger)
add_subdirectory(socket)
add_subdirectory(command_line_args_parser)
add_subdirectory(client)
add_subdirectory(server)
